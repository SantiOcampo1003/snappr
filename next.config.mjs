/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['randomuser.me'], // Agrega el dominio de donde provienen las imágenes
    },
  };
  
  export default nextConfig;